#!/usr/bin/env python3
"""基础版（课程作业 H）：温湿度测量 + 动态曲线。

这个包**刻意保持极简**：只依赖 Python 标准库；只有画图那一步需要 matplotlib。
它不 import 主项目（`rpi/health_monitor/`），所以可以整个文件夹拷走单独交作业。

模块分工：

===========================  ====================================================
模块                          职责
===========================  ====================================================
`basic.model`                一条采样记录（Reading）：缺失值保持 None，不补 0
`basic.series`               曲线数据窗口（滚动窗口 / 横轴 / 统计量，纯逻辑）
`basic.store`                CSV 存档与读回（每行立刻 fsync，拔电源不丢数据）
`basic.dht11read`            DHT11 读取（lgpio 边沿时间戳 / gpiozero / mock 三后端）
`basic.pins`                 引脚映射（只查表，不写偏移公式）
`basic.plot`                 动态曲线 + 采集主循环 + 命令行
===========================  ====================================================
"""

from __future__ import annotations

#: 基础版自己的版本号（与主项目 `health_monitor.__version__` 各自独立：
#: 基础版是"交作业的最小可用版"，主项目是完整监护系统）
__version__ = "1.0.0"

__all__ = ["__version__"]
