# 基础版验收说明（对着题目逐条过关）

> 用途：**把"作业做完了"变成一张能当场勾选的单子**。
> 每条都写清「怎么跑 → 应该看到什么 → 判据是什么」，最后有留白给验收人签字。
>
> 配套：[`README.md`](README.md)（怎么跑、常见问题）·
> [`hardware/接线表.md`](hardware/接线表.md)（**接线就这一份**：树莓派接线 + 元件接线两张表）·
> [验收记录留白](#6-验收记录留白) · [`evidence/curve_demo.png`](evidence/curve_demo.png)（曲线样张）

---

## 1. 验收前的准备（三步）

```bash
# ① 依赖（树莓派上，只需一次）
sudo apt install -y python3-matplotlib python3-tk python3-lgpio fonts-noto-cjk

# ② 接线：VCC=3.3V（物理脚 1/17）、DATA=GPIO4（物理脚 7）、GND=任意 GND（推荐脚 6）
#    四针裸传感器要在 DATA 与 3.3V 之间接 4.7kΩ~10kΩ 上拉电阻
#    接线就看这一份（两张表）：basic/hardware/接线表.md

# ③ 自检（不接硬件也能跑；接好硬件后再跑一次确认后端选到了 lgpio）
cd ~/raspberry-health-monitor/basic
python3 tools/selfcheck.py            # 10 项
python3 tools/diag_dht_line.py        # 数据线三态电平（读不到数时用它定位）
```

**自检应该看到**（开发机实测，10 项全 PASS）：

```
[PASS] 版本与导入：基础版 1.0.0（Python 3.12.5）
[PASS] 单总线解码（合成时序）：4 组合成时序全部解回原文（合成帧 82 边沿 / 41 个高电平段）
[PASS] 引脚映射（GPIO4 = 物理脚 7）：...；撞脚检查生效
[PASS] CSV 存档（缺失值不写 0）：写入/读回正确，失败行保持为空（不是 0）
[PASS] 曲线数据窗口：滚动窗口 / 统计量正确
[PASS] 演示数据 sample_demo.csv：120 行，温度 23.7~27.3 ℃，湿度 44.1~57.9 %
[PASS] 接线事实（引脚/电源/上拉/周期）：DHT11 数据脚 GPIO4（物理脚 7）、供电 3.3V（脚 1/17）、...
[PASS] 接线文档（与代码一致）：1 份接线表与代码逐字符一致，关键事实齐全，引用脚本均存在
[PASS] DHT11 后端探测：...
[PASS] matplotlib（画图依赖）：matplotlib ...
结果：全部 10 项通过 ✅
```

> 接线表本身也有一键校验（改了引脚/接线事实忘了重新生成时会报错）：
> `python3 tools/wire_docs.py --check`（生成：`--generate`）。

---

## 2. 逐条验收（题目要求 → 判据）

| # | 题目要求 | 怎么验 | 判据（达到才算过） | 实测结果 |
| --- | --- | --- | --- | --- |
| 1 | 使用树莓派 5 | 树莓派上 `cat /proc/device-tree/model` | 输出含 `Raspberry Pi 5` | ⬜ |
| 2 | 用课程学过的温湿度传感器（如 DHT11） | 树莓派上 `python3 run.py`，看终端每行读数（接线见 [`hardware/接线表.md`](hardware/接线表.md)） | 有**真实**温湿度数值（不是"未知"） | ⬜ |
| 3 | **周期性**读取 | 看终端两行读数的时间戳差 | 间隔稳定在设定值（默认 3 s，≥2 s） | ⬜ |
| 4 | 程序**保存连续采集的数据** | `ls -l data/`；`cat data/dht11_*.csv` | 有 CSV，行数 = 采样次数，含时间戳 | ⬜ |
| 5 | 用 **Matplotlib 绘制动态曲线** | 观察窗口 | 出现曲线窗口（不是静态图片），有两个指标 | ⬜ |
| 6 | 每得到一组新数据，**曲线随之刷新** | 盯着窗口看 30 秒 | 曲线**向前延伸**（不是静止），标题数字在变 | ⬜ |
| 7 | 图中明确标出温度、湿度 | 看坐标轴与图例 | 左轴 `温度 (℃)`（红）、右轴 `湿度 (%)`（蓝） | ⬜ |
| 8 | 标出**采样顺序或时间** | 看横轴 | 横轴是 `时间 (秒)` 或 `采样序号`（`--xaxis index`） | ⬜ |
| 9 | 示例输出"温度: 25.0 ℃，湿度: 58 %" | 看窗口标题 | 标题实时显示 `最新一次读数　温度: xx.x ℃　湿度: xx %` | ⬜ |
| 10 | 能观察**变化趋势** | 对传感器哈气/用手捂一下 | 曲线跟着变化（不是一条直线） | ⬜ |

---

## 3. 三条命令与"应该看到什么"

### ① 真实读取（答辩主场景）

```bash
cd ~/raspberry-health-monitor/basic
python3 run.py
```

```
数据源：DHT11 · GPIO4（物理脚 7，常用作 1-Wire / DHT 数据脚（DHT11 默认就接这里））　后端：lgpio
数据存档：/home/pi/raspberry-health-monitor/basic/data/dht11_20260924_201503.csv
[20:15:03] #   1　温度: 25.0 ℃，湿度: 58 %
[20:15:06] #   2　温度: 25.1 ℃，湿度: 57 %
...
窗口已打开：关闭窗口即退出（数据同时打印在本终端）
```

曲线窗口上应当看到：标题是"最新一次读数　温度: 25.0 ℃　湿度: 58 %"，
红蓝两条曲线随时间向前延伸，底部两行状态行写明数据源、采样周期、累计样本与范围。

> 结束时按 `Ctrl+C` 或关窗口。**已经采集的数据都已经落进 CSV**（每行即时 fsync）。

### ② 没有硬件/没有树莓派时（合成数据）

```bash
python3 run.py --mock --duration 20 --save curve.png
```

判据：20 秒后导出 `curve.png`，终端打印"共采集 N 个样本"，
曲线形状是缓慢波动的室内温湿度（24~27 ℃ / 45~57 %）。

### ③ 离线回放（验证"读 → 存 → 画"链路，用仓库里的演示数据）

```bash
python3 run.py --replay --headless --save curve.png
python3 run.py --replay            # 带窗口逐点回放（约 0.15 s 一个点）
```

---

## 4. 曲线样张（截图证据）

[`evidence/curve_demo.png`](evidence/curve_demo.png) 由下面这条命令生成（**合成数据**，
用于说明"图上应该有什么"；不代表任何真实测量结果）：

```bash
cd basic
python3 run.py --replay --headless --save evidence/curve_demo.png
```

图上必须能看到：标题里的最新读数、红/蓝双曲线、左右两套纵轴（℃ / %）、
横轴的"时间 (秒)"、底部两行状态行（数据源 / 采样周期 / 累计样本 / 温度湿度范围）。

> ⚠️ 诚实标注：这张图是 **mock 合成数据**画出来的。
> 报告里若要放"真实读数曲线"，请在树莓派上跑 `python3 run.py --no-plot --duration 300`
> 或直接 `python3 run.py --save real_curve.png` 得到真实数据版本，并注明采集时间与地点。

---

## 5. 已知限制（**请如实写进报告**）

| 项 | 状态 |
| --- | --- |
| 单总线解码逻辑（合成时序 → 解回原文） | ✅ 已单测（4 组数值 + 校验和/位不足/噪声等边界） |
| CSV 存档与只读回放 | ✅ 已单测（含"读失败必须留空"与"回放不得删文件"） |
| 动态曲线窗口（布局不重叠、标题实时刷新） | ✅ 已单测 + 已导出样张肉眼确认（开发机） |
| 接线事实与接线表（引脚/供电/上拉/周期） | ✅ 已生成 + 已机器校验（表与代码逐字符一致，检查器带注入自测） |
| 无硬件下的完整跑通（mock / 回放） | ✅ 已实测（开发机） |
| **真实 DHT11 读数** | ⬜ **未实测**（2026-09-25 真机实测：数据线被拉死到地，见 `docs/13`；接线修好后跑第 3 节① 即可验证） |
| **真机接线本身（万用表三项判据）** | ⬜ 未实测（判据与步骤见 [`hardware/_旧文档存档/02-接线图.md`](hardware/_旧文档存档/02-接线图.md) 第 5 节） |
| **树莓派上弹出窗口（桌面环境）** | ⬜ 未实测（SSH 无桌面时用 `--duration --save`；HDMI/桌面下应正常） |
| 树莓派上的 lgpio 后端选择 | ⬜ 未实测（代码路径已单测；真机会自动优先 lgpio） |

> 报告/答辩请**照上表如实区分"已测"与"未测"**：代码写完 + 单测通过 ≠ 真机跑通。

---

## 6. 验收记录（**2026-09-25 真机实测已填**）

> 下面是**真实跑出来的记录**（不是模板）：采集在树莓派 5 上、DHT11 接物理脚 7，
> 原始 CSV 与曲线图都在 [`evidence/`](evidence/) 里，报告可直接引用。

```
验收人：D.BOY（本人实测）        学号：____________     日期：2026 年 09 月 25 日

树莓派型号/系统：Raspberry Pi 5 Model B Rev 1.1 / Debian 13 (trixie)
Python 版本：3.13.5
传感器：DHT11（三针模块，丝印只有 S 与 -，中间针为 +）
数据脚：GPIO4 / 物理脚 7      接线：模块 + → 脚 1（3.3V）；- → 脚 6（GND）；S → 脚 7
接线依据：basic/hardware/接线表.md

[x] 自检 10 项全 PASS（见第 3 节输出）
[x] 接线表校验通过（python3 tools/wire_docs.py --check → 1 份接线表与代码逐字符一致）
[ ] 万用表三项：模块 VCC≈3.3V / DATA↔物理脚 7 导通 / GND↔树莓派 GND 导通
      —— 本人未用万用表；改用电平判据 + 真实读数成功来间接确认（见备注）
[x] 数据线三态电平（python3 tools/diag_dht_line.py）：LOW/变化 → 模块正常应答
      ⚠️ 注：三针模块自带上拉，空闲时数据线为高，"三态全高"属正常，判据以"能否读出数"为准
[x] 真实读数成功（终端与 CSV 摘录见下）
[x] 连续采集 ≥ 100 个样本：**111 个样本，成功 111 / 失败 0（失败率 0%）**
[x] 动态曲线窗口按要求显示（截图见 evidence/curve_real_111.png；本次为无显示器环境下 --save 导出）
[x] 曲线随新数据刷新（`--duration 330` 期间逐点追加；回放同一份 CSV 得到同形曲线）
[x] 温度/湿度/横轴/图例标注齐全（左轴温度℃红、右轴湿度%蓝、横轴时间(秒)、标题实时"最新一次读数"）
[x] 已记录采集时间、地点与室温参考值

采集时间：2026-09-25 21:14:19 ~ 21:19:51（330 秒，采样周期 3 秒）
采集地点：室内桌面（树莓派直接读传感器，未做恒温对照）
温度范围：23.8 ~ 24.2 ℃      湿度范围：57.0 ~ 58.0 %

终端前 5 行：
[21:14:20] #   1　温度: 24.2 ℃，湿度: 58 %
[21:14:23] #   2　温度: 24.2 ℃，湿度: 58 %
[21:14:26] #   3　温度: 24.2 ℃，湿度: 58 %
[21:14:29] #   4　温度: 24.2 ℃，湿度: 58 %
[21:14:32] #   5　温度: 24.2 ℃，湿度: 57 %

CSV 前 5 行（完整 111 行见 evidence/真机采集_111样本.csv）：
index,timestamp,temperature_c,humidity_percent,status,note
1,2026-09-25 21:14:20.447,24.2,58.0,ok,
2,2026-09-25 21:14:23.447,24.2,58.0,ok,
3,2026-09-25 21:14:26.447,24.2,58.0,ok,
4,2026-09-25 21:14:29.447,24.2,58.0,ok,
5,2026-09-25 21:14:32.447,24.2,57.0,ok,

备注/异常现象：
1. 期间排查过"读不到数"，最终定位为**本项目解码器的缺陷**（帧尾多一个"上拉回空闲"的
   上升沿导致错位，稳定只解出 39/40 位）与**采样间隔门禁设成了采样周期**（3.0s 门禁 vs
   2.94s 实际间隔 ⇒ 隔次被拒）。两处均已修复，修复后 9 次采样成功 9 次、
   111 次采样成功 111 次。详见开发副本 ERROR.md 的 E37 / E38。
2. 中文曲线图需要"拉丁 + 中文都全"的字体：随作业附带了
   `fonts/NotoSansCJK-Regular.ttc`（19 MB，SIL OFL 1.1），程序会自动加载；
   若删掉它，程序会退回系统字体并在缺字形时提示装 `fonts-noto-cjk`。
3. 本次为**SSH 无桌面环境**，动态窗口用 `--headless --save` 导出 PNG 验证；
   带桌面的 HDMI 窗口仍未实机确认（见第 5 节）。
```

> ⚠️ 仍然**如实标注**：上面的"真实读数"已实测；但
> **万用表三项**、**桌面环境下的动态窗口** 两项**未实测**（第 5 节标 ⬜ 的仍成立）。
