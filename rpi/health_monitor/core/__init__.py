"""业务层（与具体硬件型号无关）。

- ``collector.py``：按周期采集所有传感器，产出统一的读数集合
- ``rules.py``：报警规则引擎（纯函数，最容易单测的部分）
- ``store.py``：SQLite 历史存储
- ``config.py``：配置加载与校验

纪律：**本包内不允许出现任何具体驱动类名**（如 ``Lcd1602`` / ``Max30102``），
只能通过驱动名 + ``hal`` 数据模型工作。
"""
