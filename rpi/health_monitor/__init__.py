"""树莓派端主包。

层次（**依赖只能自上而下，禁止反向 import**）::

    hal/      契约层：Device 基类、数据模型、Mock 总线、注册表   <-- 谁都依赖它
    sensors/  输入驱动：每人数一个器件，只依赖 hal/
    outputs/  输出驱动：LCD/音箱/蜂鸣器/LED，只依赖 hal/
    core/     业务层：采集调度、报警规则、存储（依赖 hal/ 与 registry，**不依赖具体驱动类**）
    net/      对外接口：HTTP API / MQTT，只依赖 core/
    service.py 装配与主循环（唯一"知道所有零件"的地方）

一句话纪律：**core 里不允许出现 ``Lcd1602`` / ``Max30102`` 这类具体类名**，
只能通过注册表的驱动名与 ``hal`` 的数据模型工作。这样换器件不动业务逻辑。
"""

__version__ = "1.0.1"
__all__ = ["__version__"]
